// Audio Routes
// Audio serving/proxying (source-agnostic)
// Supports: external_url | local_file | none
import { Router } from 'express';

const router = Router();

// Placeholder - implemented when audio licensing is confirmed

export default router;
