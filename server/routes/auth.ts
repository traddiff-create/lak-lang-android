// Auth Routes
// Optional user auth - anonymous by default per CLAUDE.md
import { Router } from 'express';

const router = Router();

// Placeholder - only implemented if account feature is built (v2)

export default router;
